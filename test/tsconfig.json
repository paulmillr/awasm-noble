{
  "extends": "@paulmillr/jsbt/tsconfig.test.json",
  "compilerOptions": {
    // TS6 requires the previously inferred test/source output layout to be explicit.
    "rootDir": "..",
    "outDir": "compiled",
    "allowJs": true
  },
  "include": ["*.ts", "../src/targets/**/*.js"],
  "exclude": ["node_modules"]
}
